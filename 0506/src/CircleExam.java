import java.util.Scanner;
public class CircleManager {
    public static void main(String args[]) {
         Scanner scan = // 스캐너 객체 생성

         System.out.print("반지름 값 >>");
         int radius = scan.nextInt();
         Circle c = new Circle(radius); 
         System.out.println("반지름 "+ radius + "인 원의 넓이는 " + /함수호출/ + "이다.");
    }
}

class Circle{
    int radius;
    public Circle(int radius) { 

        // 생성자(radius) 값 초기화

    }
    public double Area() {   // 원의 넓이 return (원의 넓이 = 반지름반지름3.14)

        // 원의 넓이 return (원의 넓이 = 반지름반지름3.14)
    }
}